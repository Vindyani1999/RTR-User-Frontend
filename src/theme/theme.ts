import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#DC5F00",
      light:"#ffffff"
    },
    secondary: {
      main: "#373A40",
    },
  },
});

export default theme;